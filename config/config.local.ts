import { defineConfig } from 'umi';

export default defineConfig({
  // base: '/template/',
  // publicPath: '/template/',
  define: {
    BASE_URL: 'https://1234aa.com',
    __PRO__: false,
  },
});
